import pandas as pd
import numpy as np

def float_col_to_int_str(df,col):
    df=df.copy()
    df[col]=df[col].apply(
        lambda x: "" if pd.isna(x) else str(int(float(x)))
    )
    return df


df=float_col_to_int_str(df,"account_id")
