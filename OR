def supermerge(A,B,fips):
    if fips in ('53','27','41'):
        out=pd.merge(A,B,on=['UI','RUN'],how='left',indicator=True)
    else:
        out=pd.merge(A,B,on='UI',how='left',indicator=True)
    return out
