import pandas as pd

query="""
    SELECT
        COUNT(*) AS total_count,
        SUM(CASE WHEN wage < 7000 THEN 1 ELSE 0 END) AS under_7000_count,
        ROUND(
            100 * SUM(CASE WHEN wage < 7000 THEN 1 ELSE 0 END) / COUNT(*),
            2
        ) AS percent_under_7000
    FROM EMPLOYEE_WAGES
    WHERE fips = 6
"""

df=pd.read_sql(query,conn)
print(df)
