 for yr in range(2020,2026):
        df=pd.read_sql(query,conn,params={"yr":yr})
        results.append(df)

    final_df=pd.concat(results,ignore_index=True)
    final_df.columns=final_df.columns.str.lower()
    return final_df
