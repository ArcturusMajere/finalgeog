
import pandas as pd

def read_xlsx_with_sheet_column(path):
    sheets=pd.read_excel(path,sheet_name=None)
    frames=[]
    for name,df in sheets.items():
        df=df.copy()
        df['sheet_name']=name
        frames.append(df)
    return pd.concat(frames,ignore_index=True)
