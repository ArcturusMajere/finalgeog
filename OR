


def extract_all_qtrs(fipslist, start_year=2020, start_qtr=1, end_year=2025, end_qtr=3):
    out = []

    for yr in range(start_year, end_year + 1):
        for qtr in range(1, 5):
            if yr == start_year and qtr < start_qtr:
                continue
            if yr == end_year and qtr > end_qtr:
                continue

            yr_qtr = f"{yr}-{qtr}"

            for fips in fipslist:
                df = Extract_QTR(fips, yr, qtr)

                if df is None or df.empty:
                    continue

                df = df.copy()
                df["fips"] = fips
                df["yr"] = yr
                df["qtr"] = qtr
                df["yr_qtr"] = yr_qtr

                out.append(df)

    if not out:
        return pd.DataFrame()

    return pd.concat(out, ignore_index=True)

#next
from concurrent.futures import ThreadPoolExecutor, as_completed

def extract_one(args):
    fips, yr, qtr = args
    df = Extract_QTR(fips, yr, qtr)

    if df is None or df.empty:
        return None

    df = df.copy()
    df["fips"] = fips
    df["yr"] = yr
    df["qtr"] = qtr
    df["yr_qtr"] = f"{yr}-{qtr}"

    return df

def extract_all_qtrs_threaded(
    fipslist,
    start_year=2020,
    start_qtr=1,
    end_year=2025,
    end_qtr=3,
    max_workers=8
):
    tasks = []

    for yr in range(start_year, end_year + 1):
        for qtr in range(1, 5):
            if yr == start_year and qtr < start_qtr:
                continue
            if yr == end_year and qtr > end_qtr:
                continue

            for fips in fipslist:
                tasks.append((fips, yr, qtr))

    out = []

    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        futures = [executor.submit(extract_one, task) for task in tasks]

        for future in as_completed(futures):
            result = future.result()
            if result is not None:
                out.append(result)

    if not out:
        return pd.DataFrame()

    return pd.concat(out, ignore_index=True)
