import pandas as pd

def ok_vs_not_ok(df):
    d=df.copy()
    d["is_ok"]=d["state"].eq("OKLAHOMA")
    out=d.groupby("yr_qtr")["is_ok"].agg(
        Oklahoma="sum",
        not_Oklahoma=lambda x:(~x).sum()
    ).reset_index()
    return out

