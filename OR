def add_original_class(df):
    base = (
        df.loc[df["yr_qtr"].eq("2020-1"), ["ID", "class"]]
        .drop_duplicates("ID")
        .rename(columns={"class": "original_class"})
    )
    return df.merge(base, on="ID", how="left")
