import pandas as pd

def combine_categories(df):
    cat=df[['CATEGORY_1','CATEGORY_2','CATEGORY_3']].stack().reset_index(level=1,drop=True)
    df_long=df.drop(columns=['CATEGORY_1','CATEGORY_2','CATEGORY_3']).join(cat.rename('CAT'))
    return df_long.reset_index(drop=True)
